# P1 - UART Command Protocol and PC Test Runner

## Goal
Implement a UART command protocol and validate behavior with automated PC-side tests.

## Scope
- Ring buffer UART driver
- Command parser and response format
- Automated serial test runner

## Structure
- `firmware/`: AVR firmware source
- `tests/`: Host-side test scripts
- `docs/`: Protocol and test documents
